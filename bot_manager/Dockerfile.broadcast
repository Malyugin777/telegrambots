FROM python:3.12-slim

WORKDIR /app

COPY bot_manager/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY shared/ ./shared/
COPY bot_manager/ ./bot_manager/

ENV PYTHONPATH=/app
ENV PYTHONUNBUFFERED=1

CMD ["python", "-m", "bot_manager.broadcast_runner"]
